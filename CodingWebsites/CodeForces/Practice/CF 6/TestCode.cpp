#include <vector>
#include <list>
#include <map>
#include <set>
#include <deque>
#include <stack>
#include <bitset>
#include <algorithm>
#include <functional>
#include <numeric>
#include <utility>
#include <sstream>
#include <iostream>
#include <iomanip>
#include <cstdio>
#include <cmath>
#include <cstdlib>
#include <ctime>
#define FOR(i,n) for(long long (i)=1;(i)<=(n);(i)++)
#define out(i) <<#i<<"="<<(i)<<"  "
#define OUT1(a1) cout out(a1) <<endl
#define OUT2(a1,a2) cout out(a1) out(a2) <<endl
#define OUT3(a1,a2,a3) cout out(a1) out(a2) out(a3)<<endl
#define TIMEFOR(x) clock_t start_##x = clock();
#define TIMEEND(x) clock_t end_##x= clock();  \
cout <<"Time For Test_" <<#x<<":  "<<(double)(end_##x-start_##x)/CLOCKS_PER_SEC<<" seconds" <<endl;
#define LL long long
using namespace std;
int main(void)
{
	ch[0]=0;
	
	cout<<k<<endl; 
	
return 0;
}












